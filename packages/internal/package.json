{
  "name": "@projitect/internal",
  "version": "0.0.0",
  "private": true,
  "description": "Private, never-published projitect-specific Effect-extension helpers not yet upstreamed to @nunofyobiz/effect-extras (RecordX JSON-tree ops, StringX line-editing, PredicateX.isPlainObject). Bundled into the consuming package at build time.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "!**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "tc": "tsc --build"
  },
  "devDependencies": {
    "effect": "4.0.0-beta.74",
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "effect": "4.0.0-beta.74"
  }
}
